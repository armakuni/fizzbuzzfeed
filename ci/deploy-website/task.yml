platform: linux

image_resource:
  type: registry-image
  source: { repository: bitnami/git, tag: 2.23.0 }

inputs:
  - name: github-repo
  - name: website-repo

outputs:
  - name: updated-website

run:
  path: github-repo/ci/deploy-website/task.sh