platform: linux

image_resource:
  type: registry-image
  source:
    repository: jekyll/jekyll
    tag: 3.7.3

inputs:
  - name: github-repo

outputs:
  - name: generated-site

run:
  path: github-repo/ci/deploy-website/task.sh