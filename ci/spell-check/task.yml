platform: linux
image_resource:
  type: registry-image
  source:
    repository: tmaier/markdown-spellcheck
inputs:
  - name: github-repo
run:
  dir: github-repo
  path: mdspell
  args:
    - --en-gb
    - --ignore-numbers
    - --report
    - '**/*.md'